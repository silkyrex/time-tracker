{
  "name": "time-tracker-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal webhook-based backend for a voice-assisted time tracker",
  "type": "module",
  "scripts": {
    "mock:webhook": "node mock-webhook.js"
  }
}
